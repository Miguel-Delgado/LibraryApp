<Window x:Class="LibraryApp.Views.AuthorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление авторами" Height="450" Width="700">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель добавления/редактирования -->
        <GroupBox Grid.Row="0" Header="Информация об авторе" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Имя:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="FirstNameBox" Margin="5" Height="25"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Фамилия:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="LastNameBox" Margin="5" Height="25"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Дата рождения:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker Grid.Row="2" Grid.Column="1" x:Name="BirthDatePicker" Margin="5" Height="25"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Страна:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Row="3" Grid.Column="1" x:Name="CountryBox" Margin="5" Height="25"/>

                <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                    <Button Content="Добавить" Click="AddAuthor_Click" Width="80" Margin="0,0,5,0"/>
                    <Button Content="Обновить" Click="UpdateAuthor_Click" Width="80" Margin="0,0,5,0"/>
                    <Button Content="Очистить" Click="ClearFields_Click" Width="80"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Таблица авторов -->
        <DataGrid x:Name="AuthorsGrid" Grid.Row="1" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionChanged="AuthorsGrid_SelectionChanged"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="150"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="150"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat={}{0:dd.MM.yyyy}}" Width="120"/>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Удалить выбранного" Click="DeleteAuthor_Click" Width="120" Margin="0,0,5,0"/>
            <Button Content="Закрыть" Click="Close_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>