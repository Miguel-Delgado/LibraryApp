<Window x:Class="LibraryApp.Views.GenreWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление жанрами" Height="450" Width="700">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель добавления/редактирования -->
        <GroupBox Grid.Row="0" Header="Информация о жанре" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Название:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameBox" Margin="5" Height="25"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Описание:" VerticalAlignment="Top" Margin="5"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="DescriptionBox" Margin="5" 
                         Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                    <Button Content="Добавить" Click="AddGenre_Click" Width="80" Margin="0,0,5,0"/>
                    <Button Content="Обновить" Click="UpdateGenre_Click" Width="80" Margin="0,0,5,0"/>
                    <Button Content="Очистить" Click="ClearFields_Click" Width="80"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Таблица жанров -->
        <DataGrid x:Name="GenresGrid" Grid.Row="1" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionChanged="GenresGrid_SelectionChanged"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки управления -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Удалить выбранный" Click="DeleteGenre_Click" Width="120" Margin="0,0,5,0"/>
            <Button Content="Закрыть" Click="Close_Click" Width="100"/>
        </StackPanel>
    </Grid>
</Window>